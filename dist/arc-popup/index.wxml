<l-popup show="{{show}}" direction="{{direction}}" tranistion="{{tranistion}}" opacity="{{opacity}}" locked="{{locked}}" z-index="{{zIndex}}" l-class="l-class" l-bg-class="l-bg-class" bind:lintap="onArcPopupTap">
  <scroll-view scroll-y="true" class="content-arc-popup l-panel-class" style="{{_style}}">
    <slot></slot>
  </scroll-view>
</l-popup>